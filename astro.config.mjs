import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    //site: 'https://patrykopala.github.io',
    //base: '/dupa-portal'
    outDir: './docs'
});
